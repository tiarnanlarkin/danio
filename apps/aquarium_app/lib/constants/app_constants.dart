/// Barrel re-export so either import path works.
///
/// The canonical location is `lib/utils/app_constants.dart`.
/// This file exists only so `import 'constants/app_constants.dart'` resolves.
export '../utils/app_constants.dart';
